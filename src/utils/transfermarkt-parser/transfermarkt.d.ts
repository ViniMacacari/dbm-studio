export interface CountryReturn {
    countryId: number
    id: string
    logoUrl: string
    title: string
}

export interface CompetitionReturn {
    countryId: number
    id: string
    logoUrl: string
    title: string
}